import React from 'react';

interface NewsPageProps {
    simplified?: boolean;
}

export const NewsPage = ({simplified}: NewsPageProps) => {
    return (
        <div>
            NewsPage
        </div>
    );
};